#include "main.h"

/**
 * main - function that checks for uppercase character.
 * @c: input value to check
 * Return: int
 */

int main(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
		return (0);
}

